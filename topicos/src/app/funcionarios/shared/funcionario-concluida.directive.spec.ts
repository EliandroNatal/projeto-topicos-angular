import { FuncionarioConcluidaDirective } from './funcionario-concluida.directive';

describe('FuncionarioConcluidaDirective', () => {
  it('should create an instance', () => {
    const directive = new FuncionarioConcluidaDirective();
    expect(directive).toBeTruthy();
  });
});
